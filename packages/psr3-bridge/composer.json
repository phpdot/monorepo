{
    "name": "phpdot/psr3-bridge",
    "description": "PSR-3 logger bridge into the PHPdot writer pipeline.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Omar Hamdan",
            "email": "omar@phpdot.com"
        }
    ],
    "keywords": [
        "psr-3",
        "logger",
        "bridge",
        "interop"
    ],
    "require": {
        "php": ">=8.5",
        "phpdot/contracts": "^0.3",
        "phpdot/logs": "^0.3",
        "psr/log": "^3.0"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.94",
        "phpdot/container": "^0.3",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpunit/phpunit": "^13.0"
    },
    "suggest": {
        "monolog/monolog": "The usual PSR-3 implementation behind Psr3Writer when an application wants its own handler stack.",
        "phpdot/tracelog": "The file-based writer peer \u2014 install it instead when you want encrypted per-channel files rather than a PSR-3 handler stack.",
        "phpdot/container": "The #[Singleton] attributes in src stay inert until a phpdot application reflects them, so standalone consumers do not need it installed."
    },
    "autoload": {
        "psr-4": {
            "PHPdot\\Psr3Bridge\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PHPdot\\Psr3Bridge\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=256M",
        "cs-fix": "php-cs-fixer fix",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "check": [
            "@test",
            "@analyse",
            "@cs-check"
        ]
    },
    "config": {
        "sort-packages": true
    },
    "support": {
        "source": "https://github.com/phpdot/psr3-bridge",
        "issues": "https://github.com/phpdot/monorepo/issues"
    }
}
