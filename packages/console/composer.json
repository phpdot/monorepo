{
    "name": "phpdot/console",
    "description": "Attribute-driven console command discovery on Symfony Console.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Omar Hamdan",
            "email": "omar@phpdot.com"
        }
    ],
    "keywords": [
        "console",
        "cli",
        "command",
        "attribute",
        "symfony-console"
    ],
    "require": {
        "php": ">=8.5",
        "phpdot/attribute": "^0.2",
        "psr/container": "^2.0",
        "symfony/console": "^8.0"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.94",
        "phpdot/container": "^0.2",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpunit/phpunit": "^13.0",
        "swoole/ide-helper": "^6.0"
    },
    "suggest": {
        "ext-swoole": "Activates the coroutine-aware helpers in Command (guarded by extension_loaded('swoole') at runtime); everything else works without it.",
        "phpdot/container": "Autowires the container-attribute services and #[Config] DTOs declared in src (the attributes stay inert until reflected, so standalone consumers don't need it installed)."
    },
    "autoload": {
        "psr-4": {
            "PHPdot\\Console\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PHPdot\\Console\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=256M",
        "cs-fix": "php-cs-fixer fix",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "check": [
            "@test",
            "@analyse",
            "@cs-check"
        ]
    },
    "config": {
        "sort-packages": true
    },
    "support": {
        "source": "https://github.com/phpdot/console",
        "issues": "https://github.com/phpdot/monorepo/issues"
    }
}
